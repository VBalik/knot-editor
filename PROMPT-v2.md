# Промпт: Редактор топологических узлов с 3D-релаксацией

## Постановка задачи

Одностраничное веб-приложение `index.html` — один самодостаточный
HTML-файл (three.js r128 с CDN, без сборки и сервера, открывается двойным
кликом). Пользователь рисует мышью замкнутую кривую — диаграмму узла, задаёт
на пересечениях проходы «над/под», приложение строит по диаграмме
пространственную кривую и физической релаксацией приводит её к равновесной
форме: тривиальный узел распрямляется, настоящий узел принимает гладкую
объёмную форму.

## Физика релаксации (реализовано 2026-09-04, сборка v2.03)

Модель — жёсткая упругая нерастяжимая проволока без кручения. Единственный
источник сил — полная потенциальная энергия

    E = kB·Σθᵢ²  +  kR·Σ [ (u/Δ)⁴ − (u/Δc)⁴ + 4(Δ−Δc)·u⁴/Δc⁵ ],   Δ = x − s
        изгиб (Гук)     самоотталкивание чужих прядей (арк-фильтр cd·L0 > 2x)

    x  — кратчайшее расстояние между осями отрезков двух прядей;
    s  = (w + r)·u — ЖЁСТКОЕ ЯДРО: при x ≤ s энергия бесконечна (пряди не могут
         сблизиться ближе, чем на толщину плюс значение шкалы отталкивания);
    D  = w·u — диаметр трубки (он же на экране);  u = L/3000 (L — длина нити);
    w  — ползунок «Толщ.» 1–10,  r — ползунок «Отталк.» 1–10;
    Δc = 4s — обрез хвоста со сдвигом энергии и силы (гладко);
    kR = β·kB₀/N³ — сила хвоста не зависит от дискретизации (β = 2·10⁴,
         калибровка: при w=5, r=2 пряди трилистника садятся на ≈ один диаметр
         от ядра); в фазе надувания хвост усилен в 50 раз (это построение
         допустимого состояния, а не физика).

- Ограничения — геометрия, не силы: длины рёбер = L0 (точная ньютоновская
  проекция, O(N)); непроникновение гарантировано сертификатом шага (полное
  смещение вершины < половины минимального зазора).
- Спуск: L-BFGS в соболевской метрике H², точная касательная проекция, гейт
  «энергия после проекции длин уменьшилась». Старт: честный лифт (якоря
  проходов в вершинах, без сплайна), сертифицированное разлипание ⇄ проекция,
  касательный шум ≤ 30 % зазора, «надувание» ядра от 1/1,3 текущего зазора до
  чужих прядей до номинала квантованными ступенями ×1,1 (принудительно при
  застое, не выше 0,95·зазора). Если ядро растёт медленнее 3 % за 500 шагов
  или не растёт 1000 шагов — стоп «равновесие при X % толщины: нить не
  помещается при этой длине». Сдвиг ползунка во время счёта сдувает ядро до
  допустимого и надувает заново.
- МНОГОСТАРТ (2026-09-04): спуск при нулевой температуре сходится к ближайшему
  локальному минимуму, и бассейн выбирает стартовый шум, а реальная проволока
  «защёлкивается» в низкую форму за счёт инерции и тряски. Поэтому из свежего
  лифта физика запускается MULTI_START = 6 раз: раунды ≥ 2 стартуют с лифта,
  подвергнутого случайному аффинному растяжению (0,4–2,6 по случайным осям,
  длина нити восстанавливается) — аффинная биекция сохраняет тип узла строго;
  показывается форма с наименьшей полной энергией, статус «старт k из K …» и
  итог «лучший из K стартов (E: …)». Заклинивший раунд не в зачёт.
  Возобновление из покоя и сдвиг ползунка — один спуск из текущей формы.
  Проверка: трилистники из ловушек (1,71) — двойная окружность (1,001) в 100 %
  повторов, 3₁#3₁ — тройная окружность, рисунок с det 5 — двойная окружность в
  ~2/3 повторов; восьмёрка-ловушка улучшается (3,7 → 2,25), но канонические 1,61
  не гарантированы. Цена — ×K шагов.
- Тип узла: точный определитель Александера (Гаусс по двум простым модулям
  + КТО, мода по случайным проекциям) с лифта и при остановке; расхождение
  — в статусе.
- Покой — баланс сил: остаточная проецированная сила < 2 % силы изгиба кольца
  той же длины (тривиальному узлу — в 5 раз строже).
- Свойства (проверены на пресетах и 100 случайных узлах): тривиальный узел →
  точная окружность при любых w, r; толще нить или больше r → узел
  раскрывается монотонно; трилистник при тонкой нити → двойная окружность
  (Σθ² = 16π²/N); равновесная форма не зависит от N; ядро не нарушается никогда;
  у тонкой нити без контактов равновесие нестрогое (плоская долина: узел
  может съехать на несколько L0 при изменении энергии < 0,3 %).
- Проверки — `test/`: `lawcheck.js` (градиент, семантика ползунков),
  `jamcheck.js`, `infltrace.js`, кампании `randknots.js` (W, R), точный
  инвариант `rk_exact2.js`, физическая экспертиза каждого узла `rk_phys.js`,
  отчёт `rk_report2.py` → `report-fourthpower-2026-09-04.html`.

Случайные высоты частей узла (2.06, `liftFromDiagram(rnd)`): лифт из
диаграммы вынесен в `liftFromDiagram(rnd)` (`buildKnot3D` = базовый лифт +
побочные эффекты интерфейса). Объёмный лифт (2.09, 2.21): к профилю над/под
прибавляется КРУПНОЕ гладкое поле высот; с v2.21 размах поля по z в попытке —
(0.8…1.2)·размах диаграммы (было (0.3…1.0)·размах), т.е. стартовая
конфигурация «примерно как куб или шар» (по заданию): габариты ≈6×5×5…7,
σz/σxy = 0.8…1.3 (`test/cubic2.js`), тип узла лифта = тип диаграммы; базовый
лифт (показ до запуска) — две гармоники с размахом по z = размаху диаграммы.
Нормировка поля (2.22) — по размаху от минимума до максимума (не по
max|raw|), так что размах поля ровно (0.8…1.2)·размах; якоря ±A добавляют
свой разброс (полный размах по z у попыток 0.6…1.4·размаха). Число вершин N считается по
3D-длине объёмного лифта (`chooseN(L3)`), чтобы проекция воспроизводила
диаграмму точно (проверка: det лифта = det диаграммы на трилистнике, случайных
20 и 50 пересечениях). Каждая попытка серии из диаграммы (первая тоже)
строится заново: множители амплитуд якорей ×0.8…×2.0, гладкое случайное поле
δz(s) вдоль нити (4 гармоники, амплитуда (0.3…1.0)·0.5·размаха — сильно
разные высоты проекции в каждой попытке) прибавляется к профилю; в
каждом пересечении разность высот над/под доводится до расчётной суммы
амплитуд (якоря раздвигаются), поэтому порядок проходов, а с ним и тип узла,
сохраняются по построению — проекция вдоль z та же. Число вершин N у разных
лифтов может отличаться на единицы (выбор вершин по 3D-дуге): `_msLift`
обновляется на каждой попытке (`msNextLift`), лучшая попытка восстанавливается
с её N/L0 (`msRestoreBest`). Аффинное растяжение попыток осталось только для
серии после Stir (лифт — разрыхлённая форма, `_liftFromDiagram=false`).
(с v2.23 растяжение действует только в запасном варианте «растаскивание»; в режиме
«наизнанку» каждая попытка получает своё выворачивание S0.)
Проверка (`test/tries.js`): восьмёрка, 3 попытки — z_rms 0.54/0.48/0.44,
det 5 во всех, энергии 0.263/0.264/0.265.

Случайный профиль жёсткости (история 2.05–2.17; с v2.19 — отжиг, см. ниже: `stiffProfile`/`repProfile` заменены на `profilesStart`/`annApply`, разброс и конец фазы A другие) (2.05, `stiffProfile`, `_kBmul`): каждая попытка
(старт с лифта, старты серии, серия после Stir; НЕ возобновление после паузы)
после раскрытия ядра сначала спускается с жёсткостью, гладко и случайно
меняющейся вдоль нити: множитель exp(σ·g), g — сглаженный (I+300Δ)⁻¹ белый шум,
нормированный на [−1,1], σ = ln 8 (v2.08: разброс ×⅛…×8; было ×⅓…×3). Разные
части нити стягиваются к минимуму с разной силой — пути спуска различаются,
вероятность найти глобальный минимум выше. Там же (v2.08) — случайный профиль
хвоста отталкивания `_kRmul` (`repProfile`, σ = ln 4: ×¼…×4): множитель пары
сегментов √(m_i·m_j) только для хвоста 1/Δ⁴, жёсткое ядро s неизменно
(топологическая безопасность и закон «бесконечно при w+r»); действует в той
же фазе A и снимается вместе с профилем жёсткости. Статус: «varied
stiffness/repulsion». Цена — попытки длиннее (пресеты 500–1400 шагов). Фаза A длится до покоя или 1500 шагов, затем
истинная однородная энергия (`stiffPhaseEnd`: память L-BFGS и окна покоя
заново) до настоящего равновесия; энергии попыток и «Min» берутся только из
фазы B; в статусе — «varied stiffness». Проверка (`test/diversity.js`):
случайный 20-перекрёстный узел (det 145) — пять разных энергий 0.403…0.555;
трилистник/восьмёрка/узел пользователя — все попытки в одном бассейне.
Контрольная точка (v2.17, `ckObserve`/`trueEnergy`, `CK_EVERY`=25): в фазе A
раз в 25 шагов считается истинная однородная энергия текущей формы (профили
на время выключены); лучшая по ней форма запоминается (`_ckE/_ckVerts`), и
если к концу фазы A истинная энергия выше, `stiffPhaseEnd` возвращает нить в
запомненную форму (`__knotTrace().ck` = {restored, Eck, Ecur}). В статусе
«E» во время фазы A — истинная энергия (`shownE`), а не искажённая профилями.
Повод: пользователь заметил, что энергия по ходу часто ниже конечной.
Проверка `test/ckpt.js`: восстановление сработало на 4 из 4 узлов, финалы
ниже (0.3009 против 0.3041, 0.1262/0.1269, 0.2235/0.2291, 0.1242/0.1249).
Отжиг профилей (v2.19, по заданию «расширить вариативность в самых широких
пределах и постепенно уменьшать до значений ползунков»): разброс стартует с
жёсткости ×1/32…×32 (`STIFF_SPREAD=ln 32`) и хвоста отталкивания ×1/16…×16
(`REP_SPREAD=ln 16`; ядро неизменно) и за `STIFF_STEPS`=1500 шагов фазы A линейно
по логарифму сходит к нулю: множители exp(σ(t)·g), σ(t)=σ₀·(1−t/T),
пересчёт каждые `ANN_EVERY`=10 шагов (`annApply`: энергия точки заново, память
L-BFGS остаётся). Покой в фазе A не завершает её, а сдвигает отжиг на
`ANN_JUMP`=T/8 шагов вперёд (`annQuiet`, память спуска и окна покоя заново);
покой в последних T/8 шагах или σ=0 — `stiffPhaseEnd` (чекпойнт v2.17
действует как прежде); детектор «энергия не убывает» (eFlat) в фазе A
отключён — во время отжига дрейфует сама функция, покой только по силе. Так путь
спуска каждой попытки начинается в сильно искажённом ландшафте и постепенно
возвращается в истинный. Статус: «varied stiffness/repulsion ×R», R —
текущий размах жёсткости (32 → 1). Стенд: `window.__ann={stiff, rep, steps,
every, jump, hard}` (hard — σ₀ постоянна всю фазу A, как в 2.08; с
stiff:ln 8, rep:ln 4 — ровно поведение 2.08), `__knotTrace().ann`,
`test/anneal.js` (трасса), `test/annexp.js` (сравнение настроек на наборе
узлов: `ANN='{…}' REP=3 TRIES=5 KNOTS=… node harness.js annexp.js`).
Замер (2026-09-09, 7 узлов × 3 серии × 5 попыток = 105 попыток на
конфигурацию; узлы: восьмёрка, узел пользователя 20 пересечений det 49, узел
пользователя det 11 при ползунках 10/10/2 и 5/2/9, случайные 12/16/20
пересечений): доля попыток, пришедших в лучший найденный бассейн (±1 %) —
2.08 (×8 без отжига) 33 %, отжиг ×8 44 %, отжиг ×32 44 %, отжиг ×100 37 %,
отжиг ×32 за 3000 шагов 38 %; доля «плохих» попыток (>15 % над минимумом) —
7 / 13 / 13 / 16 / 9 %; серий с минимумом в лучшем бассейне — 16 / 16 / 17 /
14 / 15 из 21; время — 1020 / 757 / 774 / 820 / 935 с (после отжига фаза B
короткая). Лучший найденный минимум по всем конфигурациям одинаков в пределах
0.5 % на всех узлах, кроме случайного 20-перекрёстного (det 7): его редкий
бассейн 0.155 без отжига находился 2/15, с отжигом ×32 — 0/15 (в сумме по
отжигам 3/120). Итог: отжиг с ×32 оставлен по умолчанию — чаще попадает в
лучший бассейн и быстрее, но даёт больше явно плохих попыток; шире ×32 (×100)
хуже, медленнее отжиг — не лучше. Абсолютный минимум по-прежнему определяется
в основном случайным лифтом, а не профилем.

Физика запускается ТОЛЬКО кнопкой «▶ Physics» (v2.05): смена ползунка в покое
меняет параметры, но спуск не стартует; во время счёта ползунки действуют на
лету (серия попыток при этом перезапускается с лифта).

Быстродействие (2.11). Профиль шага (V8, N≈490, без обёрток): 2.4 мс/шаг, из
них ~60 % — расчёт расстояний между отрезками для пар в радиусе хвоста
(5·s + 1.5·L0), число таких пар задано физикой. Сделано: `_closestSeg` без
аллокаций (общий буфер), перебор пар через пространственную сетку ячеек
размера R по средним точкам отрезков (counting-sort в типизированных массивах,
27 соседних ячеек; при N<64 или флаге `_pairBrute` — полный перебор, эталон
для проверки: `test/gridcheck.js`, расхождения ~1e-16). Выигрыш ~12 %.
Главный резерв — параллельные попытки: каждая попытка серии считается в своём
Web Worker (`parStart/parLaunch/parMessage/parFinish/parCancel/parRestart`):
воркер — безголовая копия этой же страницы (`__PAGE_SRC` = текст скрипта,
заглушки DOM и WebGL/OrbitControls как в стенде, THREE из CDN через
`importScripts`), получает диаграмму (raw + проходы) или лифт (после Stir; с v2.24 — ещё S0, det,
занятые «дыры» и семя: k-й воркер сам выворачивает S0 вокруг k-й дыры) и
ползунки, строит свой случайный лифт, гоняет `relaxStep` до покоя, шлёт
прогресс (E, надувание, вершины) каждые 100 шагов и итог (E, вершины, det,
окружность/заклинивание). Главный поток: пул ядер−1 (≤8) воркеров; в 3D —
ОДНА попытка (наименьший номер среди идущих) до её конца, без перескоков,
снимки каждые 12 шагов (v2.12; раньше «лучшая из идущих» раз в 100 шагов —
рывки); v2.15: между двумя последними снимками положения вершин линейно
интерполируются по времени (`parInterp`, сглаженный интервал прихода снимков,
задержка один интервал), главный вид перестраивается каждый кадр, окна всех
попыток — через 1–2 кадра облегчённой трубкой (N сегментов × 8 граней);
главный вид во время любого счёта — трубка средней детализации (2N × 12), в
покое — полная (3N × 24, `rebuildTube(true)` в stopPhysics). Замер в видимой
панели при N=490 и 4 живых окнах: кадр 16.7 мс (p90), перерисовка 6.5 мс
(p50). Окна всех попыток создаются на старте серии с подписью «running» —
видно, что попытки считаются одновременно; по завершении подпись «E …»;
окружность у тривиального узла останавливает остальные, в конце —
минимум и обычный финальный статус (+ « · N s parallel»). Пауза = отмена
серии (лучший результат остаётся, далее один спуск), ползунок во время серии =
перезапуск, новая диаграмма/Clear = отмена; при ошибке воркера до первого
результата — последовательный путь (`window.__noWorkers`). v2.14 (ошибка
пользователя «ERROR: Script error.» ×N при открытии страницы как файла в Safari):
при `file://` воркеры не используются вовсе (Safari не даёт blob-воркеру грузить
three.js с CDN, ошибки всплывали в баннер страницы и серия зависала на
«Stirring…»); `worker.onerror` гасит событие (`preventDefault`, без баннера);
`importScripts` в воркере под try/catch с явным сообщением; сторожевой таймер
4 с без ответа → отказ; при отказе «running»-окна серии удаляются, перенесённое
окно №1 остаётся, серия идёт последовательно с пометкой «workers unavailable,
sequential». Отладка: `window.__breakWorkers='load'|'throw'`. В стенде Node
(`Worker` нет) — прежний последовательный путь. Проверено в браузере: 5 попыток
на 10 ядрах — 16 с, два разных бассейна (0.333/0.337 и 0.365/0.367); серия
после Stir — параллельно с той же пометкой «stirred». Исправление 2.22 (найдено
ревью): предпросмотр параллельной серии (`parRender`) подменяет `verts`/`N`
вершинами воркера без пересоздания буферов, а `msRestoreBest`/`_setVertsFrom`
судили о нужде в `allocBuffers` по N — при совпадении N лучшей попытки с N
предпросмотра и отличии от N базового лифта буферы оставались старого размера
(NaN/TypeError на следующем Physics или Stir). Теперь ключ — размер буферов
(`_fx.length`), и `parRender` сам пересоздаёт буферы при смене N. Телеметрия параллельных
попыток не пишется.

Проверка второго порядка (2.03, `minMode`/`saddleEscape`): покой по силе ещё не
минимум — многократно свёрнутые кольца и прочие сёдла эластики имеют нулевой
градиент. Перед объявлением равновесия (при раскрытом ядре) ищется минимальная
мода гессиана на многообразии длин: Ланцош с полной реортогонализацией для
B·H_T·B, B = P·(I+aΔ)⁻¹·P (конгруэнтно H_T — знаки собственных значений те же,
спектр сжат, сходимость за 40–120 итераций с контролем значения Ритца); H·u —
конечная разность проецированного градиента; стартовый вектор детерминирован,
остановка — два подряд стабильных (2 %) значения Ритца. Порог — в физических
единицах: λ₂ = 18·(2π)⁴·kB/(N⁴·L0²) (мягчайшая мода m=2 кольца той же длины,
`lamRing2()`; в норме вектора вершин мягкие моды ∝N⁻⁴, а λ_max от N не зависит,
поэтому порог относительно λ_max не инвариантен к дискретизации). Если
λ_min < −0.02·λ₂, делается пробный шаг вдоль моды (знак по силе, амплитуды
{1, ⅓, ⅑}·min(0.35·gmin, 0.5·L0), проекция длин, сертификат полного смещения как
в шаге), принимаемый только при E↓ и целом ядре; спуск продолжается (до 8
выходов за старт). Проверено (`test/mmcheck2.js`): двойные/тройные окружности
при N=300 и 600 дают λ_min = −(0.1…1.25)·λ₂, окружность ≈ 0 (не помечается).
Найденный на прогоне пользователя случай (det 11, 20 пересечений, w=r=10):
состояние «три витка + петля» — точка баланса изгиба и хвоста отталкивания
(fRel 3e-7) с мелким седлом (λ_min = −9·λ₂); выход даёт E↓ на 9e-5 и соседний
минимум той же формы; при Repel 1 и при значениях по умолчанию — тоже локальные
минимумы (7.6–7.7× энергии кольца) с λ_min ≈ 0, все 5 стартов сходятся к одной
энергии. То есть эта форма — настоящий локальный минимум заданной энергии
(контактно-стабилизированная), а не недосчитанное состояние. Ограничение: контактно-стабилизированные
состояния (все моды ≥ 0) — настоящие локальные минимумы заданной энергии и
проверкой не снимаются; для них работает только многостарт.

Многостарт прекращается досрочно, если какой-то старт пришёл к окружности
(тривиальный узел, E<1.3·E_круга): окружность — глобальный минимум, остальные
старты не считаются (статус «circle on start k of K, remaining starts skipped»).

## Интерфейс

Два окна рядом; все управляющие элементы — в компактных тулбарах в одну строку
над окнами (одинаковая высота слева и справа), сами окна чистые.
Оформление v2.16 («Neon Lab», выбрано из трёх независимых вариантов по
скриншотам): глубокий тёмный фон (#070a10), панели-карточки со скруглением и
отступом 8 px, плавающие тулбары, шрифт Geist (Google Fonts, `@import` с
системным запасным стеком), точечная сетка на 2D-холсте (CSS-градиент),
акценты бирюза→фиолет с мягкими свечениями (≤ .5), чипы-пилюли с
ползунками (светящийся бегунок), единая кнопка: зелёная «▶ Physics», красная
пульсирующая «⏸ Pause», фиолетовая «Stir», приглушённая «Stirring…»; плашка
статуса стеклянная; окна попыток — карточки с прозрачным телом (3D рисуется на
холсте под ними), рекордное — зелёная светящаяся рамка. Все функциональные
правила (скрытые элементы, состояния, FLIP-переходы окон, компактный режим
< 1240 px) сохранены. Пробные варианты оформления (v2.16 и v2.18: glass/minimal/neon, light/spatial/
brutal) удалены по решению пользователя (2026-09-09); остался только основной
файл.
Цвета холстов (v2.18) задаёт стиль страницы через CSS-переменные `:root`,
которые скрипт читает один раз при старте (`theme()`, значения по умолчанию —
прежние): `--draw-dot` (точечная сетка 2D), `--draw-stroke-a/-b` (градиент
замкнутой диаграммы), `--draw-open` (незамкнутая линия), `--draw-start`,
`--draw-start-fill`, `--draw-near`, `--draw-near-fill` (кружок старта и его
«можно замкнуть»), `--draw-ring`, `--draw-ring-fill` (кольца пересечений);
`--gl-bg` (фон 3D-сцены и окон попыток), `--gl-floor` (пол), `--gl-grid1/2`
(сетка) — для three.js только `#rrggbb`. Трубка окрашена картой напряжения
(зелёный→красный) и стилю не подчиняется.


### Файл программы
С v2.25 файл программы называется `index.html` (по решению пользователя; прежнее
имя `knot_editor.html` больше не используется — стенд `test/harness.js` читает
`index.html`, запасной путь к старому имени оставлен). Публикация на Pages —
этот же файл, без копирования.

### Версия интерфейса
Заголовок левого окна — «2D Editor X.Y»: X задаёт пользователь (сейчас 2),
Y — порядковый номер сборки, растёт на 1 с каждой новой сборкой (сейчас 3.3 — мажорная версия 3, далее 3.4, 3.5…;
в коде `APP_VER_MAJOR`, `APP_VER_MINOR`; `window.__buildTag` = «vX.Y»).
С v2.02 все элементы интерфейса (подписи, подсказки, статусы, заголовок
страницы) — на английском; комментарии в коде остаются русскими.
Номер версии стоит сразу после «2D Editor» сплошным светлым цветом (v2.20:
раньше span версии с opacity внутри градиентного текста background-clip:text
выпадал из маски и был невидим); в узком окне (< 1240 px) заголовок «3D View»
скрывается, а «2D Editor <версия>» остаётся.

### Левое окно — 2D-редактор диаграммы
Случайная диаграмма (кнопка «Random») строится на решётке; с v2.35 прямые углы
решётки заменены дугами (`_gdCurve`: скругление радиусом до 0.42 клетки, не дальше
45 % смежного ребра; пересечения лежат в узлах решётки не ближе целой клетки от
углов и не затрагиваются) — по просьбе пользователя «прямые углы на диаграмме 2D и
на 3D при построении проекции по диаграмме заменить на округлые»; 3D-лифт следует
2D-кривой, поэтому тоже без углов.
- Рисование: зажать мышь и вести; для замыкания вернуться в стартовый кружок
  (авто-замыкание). Кривая сглаживается (Catmull-Rom).
- Самопересечения находятся автоматически. Отрисовка классическая: верхняя
  прядь сплошная, нижняя — с небольшим чистым разрывом, целиком внутри серого
  кольца-хендла (разрыв ~полширины линии с каждой стороны, ровные торцы,
  диаметр кольца не увеличивается).
- Клик по кольцу переключает над/под; 3D-вид обновляется сразу.
- Тулбар (v2.02): «Random» с полем числа пересечений 1–100 (v2.05: без
  спиннера, по центру, ввод ограничивается 1–100 с записью обратно) и «Clear».
  Случайная диаграмма (`randomKnot`) — **сеточная диаграмма** (grid diagram):
  n×n сетка, в каждом столбце вертикальный отрезок X→O, в каждой строке
  горизонтальный O→X, пересечения лежат только в узлах сетки. База —
  «торическая» сетка (σ=id, τ=сдвиг на s, gcd(s,n)=1, ≈n²/4 пересечений),
  случайность — j·n транспозиций в σ и τ; из тысяч кандидатов (одна компонента,
  комбинаторный счёт пересечений) берётся точное попадание в заданное число с
  максимальной случайностью. Затем растяжение под пропорции панели (≤1.5×),
  гладкий диффеоморфный варп (|∇u|≤g) и поворот — комбинаторику не меняют;
  проходы над/под случайны. **Правило разнесения**: расстояние между любыми
  двумя пересечениями ≥ 1.5 радиуса кружка-маркера (`CROSS_R`=14 px → 21 px);
  оно обеспечивается шагом сетки (n ≤ 0.9·min(W,H)/(1.03·21)+1) и проверяется
  по факту (`_minCrossDist`); при нарушении варп/поворот уменьшаются, в пределе
  — чистая сетка. Если окно слишком узкое для запрошенного числа, статус
  предупреждает «requested N (window limit n×n)»; если сетка допустима, но
  точного попадания среди кандидатов нет — поиск продолжается до 60000
  кандидатов/250 мс, иначе «(no exact grid found)». Генерация 5–35 мс.
  Пресеты 3₁…7₁, «Last», чипы «×N» и «type» скрыты (элементы остаются в DOM для
  стенда тестов).

### Правое окно — 3D-вид
- Кривая отображается трубкой (TubeGeometry) с бликами; парит над полом с
  сеткой и отбрасывает мягкую тень; пол всегда чуть ниже нижней точки узла.
- Освещение со всех сторон: узел никогда не тонет в темноте и не «выбеливается»,
  цвет и объём читаются при любом ракурсе.
- Камера орбитальная: вращение мышью, зум колесом; узел всегда целиком в кадре
  и по центру окна (авто-кадрирование, панорама отключена).
- Тулбар: «Thick.» w 1–10 (физический диаметр трубки, он же на экране);
  ползунок «Stiff.» 1–20 (v2.10, было 1–10); «Repel.» r 1–10 (v2.35: жёсткое ядро
  снова на расстоянии (w+r) единиц, как в исходном ТЗ, а просьба «отталкивание в 5
  раз сильнее» (v2.21) исполнена силой хвоста — `REP_FORCE=5` в `kRep`; в v2.21–2.34
  ядро было (w+5r): невидимая запретная зона в 5–18 раз шире нарисованной трубки, и
  узлы, зрительно помещавшиеся с большим запасом, получали «jammed»/«does not fit» —
  все жалобы на это шли именно отсюда; единица — 1/3000 длины нити); кнопка «▶ Physics» /
  «⏸ Pause»; после финала всех попыток та же кнопка становится «Stir»
  (v2.06, фиолетовая, `_playStir`/`stirEnable`): нажатие разрыхляет узел и
  повторяет попытки, во время разрыхления кнопка «Stirring…» неактивна (в режиме «наизнанку» 2.23 —
  лишь на один кадр: выворачивание мгновенно), затем
  снова «⏸ Pause» и по финалу «Stir»; новая диаграмма и Clear возвращают
  «▶ Physics». Поле «Tries» (v2.06, ранее «Starts») — число попыток 1–20 (по
  умолчанию 5, `MULTI_START`), применяется к следующему запуску. Легенда «stress», «OBJ» и «Telem» с v2.01 скрыты (код экспорта и
  телеметрии сохранён).
- Статус — плашка в ЛЕВОМ нижнем углу 3D-окна (v2.03: начало текста не
  прыгает; длинный текст обрезается многоточием) с пульсирующим индикатором:
  янтарный во время релаксации, ЗЕЛЁНЫЙ по завершении. Формат во время счёта
  (v2.02): `Min <минимальная полная энергия по всем стартам> · <процент
  работы> · E <текущая энергия>`, далее «start k/K» при многостарте и «opening
  contacts N%» в фазе надувания (`runStatus()`). Процент — (завершённые
  старты + оценка внутри старта)/K; внутри старта надувание даёт первые 25 %,
  дальше — максимум из спада силы к порогу покоя (лог-шкала) и доли шагов от
  средней длины завершённых стартов (`progUpdate`, монотонно, <100 % до
  остановки). Для тривиального узла знаменатель — текущий номер старта
  (окружность обычно на первом). Путь файла телеметрии в статус не выводится
  (лог сохраняется молча). Итог (v2.03, `finalStatus()`): `Min: <минимум> (1: E₁, 2: E₂,
  …)` — энергии всех попыток по порядку (заклинившая — «—»), затем только
  примечания: « · circle», « · local minimum (not a circle)», « · remaining N
  starts skipped» (окружность найдена раньше), « · ⚠ does not fit at N%
  thickness (reduce Thick./Repel.)», « · ⚠ knot type changed (det a → b)».
  Пауза: текущий статус + «paused»; возобновление после паузы ВНУТРИ серии
  попыток продолжает серию (без нового растяжения). Смена ползунка во время
  серии, когда хотя бы одна попытка уже завершена, перезапускает серию с лифта
  (энергии при других kB/kR несравнимы); окна очищаются. Совет «tight fit
  likely (N crossings; draw larger…)» — суффикс статуса на весь прогон.
- Окна результатов попыток (v2.03): после каждой попытки в поле 3D-вида
  появляется квадратное окно «#k · E …» с формой этой попытки (своя сцена,
  камера и OrbitControls на div окна — вращение мышью, зум колесом); окна
  рисуются на общем WebGL-холсте через scissor-области после главной сцены
  (`msResultAdd`, `msRender`, `_msWins`). Кнопок у окон нет (v2.05): клик по
  окну (без протяжки — протяжка вращает) плавно раскрывает его на всё 3D-поле
  (CSS-переход 0.35 с, `msExpand`: spacer + absolute, остальные окна скрыты), в
  заголовке появляется крестик «✕», который сворачивает окно обратно
  (`msCollapse`). Окна очищаются при новой диаграмме, смене прохода и «Clear»;
  возобновление после паузы их сохраняет. Заклинившая попытка — окно
  «jammed». Рекордная (минимальная по энергии) попытка выделена зелёной рамкой
  (`msMarkBest`, класс `.best`; обновляется по мере завершения попыток).
  Камера окна следит за размером узла (v2.25, `msWinFit`): пересчёт при
  изменении радиуса больше чем на 6 % (как у главного вида), направление
  взгляда пользователя сохраняется, цель — центр масс формы. С v2.27 кадр
  пересчитывается и при смене ПРОПОРЦИИ окна (`w.fitA`, проверка в `msRender`:
  раскрытие окна на всю панель, сворачивание, изменение размера окна браузера) —
  иначе в узкой панели раскрытое окно обрезало узел ровно так же, как в
  исходной жалобе. Ножницы отрисовки окна пересекаются с прямоугольником дока
  и холста: окно, не поместившееся в док, больше не рисует непрозрачный кусок
  сцены поверх главного вида. Скрытые окна (под раскрытым) не перестраиваются. До этого
  расстояние подбиралось один раз по стартовому лифту, и растущий узел
  (тривиальный узел распрямляется в окружность радиуса L/2π — измерено ×2…×3)
  вылезал за кадр: в окне была видна лишь часть нити (жалоба пользователя со
  скриншотом). Окна попыток, не доведённых до конца (найдена окружность →
  «remaining N tries skipped», отказ воркера, пауза), подписываются «skipped» /
  «failed» / «stopped» (`msWinCap`), а не остаются с «running». Энергии попыток
  больше не обрываются на первой незавершённой (попытки в параллельном режиме
  финишируют вразнобой): список хранит места попыток, незавершённая — «—» на
  своём номере, число пропущенных считается по дыркам (v2.27). Удаление
  раскрытого окна снимает состояние «раскрыто» (иначе раскрытие переставало
  работать до конца сеанса), а перезапуск серии по ползунку убирает окна
  прерванной серии.
  При Stir (v2.13, `msWinsKeepBest`) окно рекордной попытки плавно переезжает
  в левый верхний угол (FLIP-анимация 0.5 с) и становится №1, остальные окна
  исчезают; её энергия и форма переносятся в новую серию (`_msCarry`,
  `_msOffset=1`): новых попыток строится Tries−1 (№2…Tries), их энергии
  сравниваются с перенесённой, итоговый список — все Tries значений. В безголовом стенде окна не создаются (данные сохраняются).
- Режим «Stir» единой кнопки (v2.04 — отдельная кнопка, v2.06 — режим
  «▶ Physics»): доступен после окончательного завершения всех попыток (любой
  финальный статус). Действует на лучший из полученных вариантов (он и
  показан в 3D-окне после финала).
  v2.23 «выворачивание наизнанку» (по заданию «придумать новый способ Stir,
  принципиально меняющий 3D-конфигурацию, чтобы появлялась возможность найти
  новый минимум»). Панель из четырёх прототипов (2026-09-10, стенд, 8 диаграмм
  24…66 пересечений, протокол: серия 3 → метод → серия 2 + лучшая; сравнение
  с прежним Stir и с 2 свежими попытками): растаскивание частей (2.21) и
  перепроецирование (новая диаграмма того же узла со случайного ракурса, проходы
  из глубины, новый лифт) — та же долина энергии; перевороты петель вокруг
  хорды — у релаксированных узлов нет свободных петель, повороты блокируются
  соседями; конформная инверсия — другой бассейн в 4 из 8, минимум ниже в 2 из 8
  (на 4 %), det сохранён в 16/16. Внедрена инверсия: центр c — «дыра» узла (узел
  решётки 15³ внутри выпуклой оболочки, максимально далёкий от всех отрезков,
  вне пустых шаров уже использованных центров), отражение в случайной плоскости
  через c и x → c + d²(x−c)/|x−c|² (композиция сохраняет ориентацию, иначе
  зеркальный узел) — гомеоморфизм R³∖{c}: тип узла сохраняется по построению,
  внутренние дуги уходят наружу. Ломаная дробится (8 на ребро), образ
  ресэмплируется по дуге в те же N вершин, масштабируется к длине N·L0, поправка
  длин; приём — вложенность (gmin>0) и det (`_detRobust(3)`) как до Stir; иначе
  другой центр/плоскость (до 6 раз), затем запасной вариант — растаскивание
  2.21. Stir мгновенный; серия стартует с вывернутой формы, а каждая следующая
  попытка (и каждый воркер в параллельном режиме) получает СВОЁ выворачивание
  исходной формы S0 вокруг другой «дыры» (`_stirS0`, `_stirHoles`,
  `stirRemapOnce`, `stirRemapLift`, `_par.lifts`); аффинное растяжение попыток
  при этом не применяется. Пометка серии « · stirred (inside out)»,
  `__knotTrace().stir` = {mode:'invert'|'pull', remap:{c, d_R, disp_R, det,
  gmin_L0}, …}. Стенд `test/stir8.js`: трилистник/восьмёрка/случайные 12 и 30 —
  det сохранён, серии сходятся, на случайном 30 вторая попытка нашла минимум
  на 4.5 % ниже.
  Замер «наизнанку» (сборка 2.23, `test/stirexp.js`, 17 из тех же 50 диаграмм
  20…98 пересечений, ползунки 3/1/9, серия 5 → Stir + 4 попытки, контроль — 4
  свежие попытки): минимум ниже более чем на 0.5 % на 8 из 17 (более 1 % — 5,
  более 2 % — 3, максимум 4.0 %), среднее 0.88 %; прежнее растаскивание на тех
  же диаграммах — 5 из 17, среднее 0.44 %; контроль — 3 из 17, среднее −1.7 %.
  det сохранён во всех 17. Выигрыш не зависит от размера (20–44: 3/6, 45–69:
  2/5, 70–98: 3/6). Дополнительные партии панели (conformal_b/c, 14 диаграмм,
  серия 3): ниже на 5 из 14; изотопия (гладкие потоки, 8 диаграмм) — 0 из 8.
  Ревью 2.24 (37 агентов) и исправления: (1) ресэмплинг образа по дуге НЕ был
  топологически безопасен (хорда срезает сжатые области образа, det неполный
  инвариант — измерено изменение типа на сплюснутом лифте) — добавлен
  сертификат `stirResampleCert`: линейная гомотопия дробной ломаной → Y
  безопасна, если каждая вершина дробной ломаной сдвигается меньше чем на 0.45
  её зазора до чужих (дальше 2 исходных рёбер) отрезков (сетка ячеек, ~0.1 с);
  сплюснутые/нерелаксированные лифты отвергаются («cert»), релаксированные
  узлы проходят; `_detRobust===null` — отказ; (2) отвергнутая «дыра»
  исключается при повторе (отражение при том же центре даёт конгруэнтную
  копию), без новой дыры — отказ «centre»; det S0 считается заново
  (`_detRobust(3)` формы S0, а не det лифта последней попытки); при повторном
  Stir с тем же S0 использованные дыры не сбрасываются; (3) воркеры
  выворачивают S0 сами (получают S0, det, занятые дыры и семя; k-й воркер —
  k-я дыра), главный поток делает одно выворачивание, отложенное на кадр
  (`_stirPending`, надпись «stirring… turning inside out»); лифт в воркере
  ставится без сплайна (`_setVertsFrom`, прежний `__knotSetVerts` → Catmull-Rom
  был несертифицирован); (4) вывернутая форма показывается сразу
  (`rebuildTube(); fitCamera(true)` в stirFinish); попытка без свободной дыры
  получает мягкое растяжение первого выворачивания (как воркер); смена
  ползунка в последовательной серии после Stir нумерует окна с №1
  (`_msOffset=0`); причины отказов — `__knotTrace().stir.rej`, при запасном
  варианте — в пометке «inside-out rejected: …»; подсказка кнопки обновлена.
  v2.21 (по заданию «только серьёзное растаскивание в разные стороны отдельных
  случайных частей узла»; с v2.23 — запасной вариант): стадии «надувания» больше нет. Нить режется на 3–6
  случайных частей, каждая тянется в свою случайную сторону ОДНИМ полем весь
  Stir (гладкое вдоль нити (I+200Δ)⁻¹, |поле| ≤ 1, плюс сила отталкивания
  весом 1 при усиленном ×50 хвосте), пока наибольший сдвиг вершины от начала
  не достигнет `STIR_AMP`=1.0·R (R — радиус узла) или не кончатся
  `STIR_STEPS`=600 шагов. Контакты: в близких чужих парах (x < s+2.5·L0)
  сближающая составляющая поля убирается симметрично (до 4 раундов, чередуясь
  с проекцией на жёсткие длины) — упёршаяся часть тащит соседа за собой.
  Сертификат шага ЛОКАЛЬНЫЙ: вершине разрешён сдвиг ≤ 0.35 её зазора (min по
  двум смежным отрезкам, все пары cd>2, не больше thr=s+3·L0; у ядра чужой
  пряди — не больше половины свободного зазора, пол 0.15·L0), общий кап
  min(L0, 0.35·thr); шаг принимается, если E конечна (чужие пряди вне ядра) и
  полное смещение каждой вершины после поправки длин < 0.5 её зазора — пряди
  не могут пересечься; иначе откат и кап ×0.35 (3 попытки), после 8 отказов
  подряд — новые направления. Допуск входа в ядро при сохранении локального
  сертификата проверялся и отвергнут: зазоры схлопываются до 0.001·s и всё
  стопорится. Ревью 2.22 закрыло формальную дыру сертификата: пары,
  невидимые для сетки перебора (радиус 5s+1.5·L0), могли быть ближе thr при
  s < 0.6·L0 — при сборе зазоров для Stir радиус сетки ≥ s+4.5·L0 (`energyGrad`,
  `_segGap`), так что невидимые пары всегда дальше thr. Статус «stirring… pulling parts apart (NN%)», пометка серии
  « · stirred (parts moved X·R)»; «too tight» — если сдвиг < 0.5·L0.
  Замер (`test/stir7.js`, 3 прогона): трилистник/восьмёрка/случайный 12 —
  сдвиг 1.0·R за 30–420 шагов (радиус ×1.06…×1.87), плотный случайный 12 при
  w=10 r=10 — 0.5…1.0·R; det до/после равен всегда; серия после Stir
  приходит к прежним минимумам, иногда ниже (0.696 против 0.739).
  Замер 2026-09-10 по заданию «50 диаграмм 20…100 пересечений: даёт ли Stir
  после Physics меньший минимум» (`test/stirexp.js`, ползунки 3/1/9 — при
  штатных 5/2 узлы от 60 пересечений не помещаются; серия 5 попыток → Stir и
  4 новые попытки с переносом лучшей; контроль — 4 свежие попытки без Stir):
  Stir снизил минимум более чем на 0.5 % на 10 диаграммах из 50 (более 1 % —
  на 3, максимум 3.4 % при 44 пересечениях), в среднем на 0.29 %, медиана
  0.07 %; контроль — на 11 из 50, но на 13 хуже (лучшая не переносится),
  в среднем −0.18 %. Тип узла (det) сохранён во всех 50. Вывод: Stir 2.21 —
  это «ещё четыре попытки с сохранением лучшей», принципиально новых
  конфигураций он не даёт; релаксация возвращается в тот же бассейн.
  История v2.07–2.20 (для понимания старых тестов `stir.js…stir6.js`):
  две стадии: A «надувание» —
  жёсткое ядро растёт вслед за зазором чужих прядей (ратчет без диагноза «не
  помещается») до цели max(2.5·s, 0.25·R узла) (≤30·s) при усиленном (×50)
  отталкивании, спуск по этой энергии под обычными сертификатами — нить сама
  раздвигает все контакты, узел заметно расширяется (до 400 шагов, ранний
  выход по достижении цели; статус «stirring… loosening: core ×k of K»); B —
  ядро уменьшается вдвое от достигнутого (запас хода) и 300 шагов случайного
  поля (см. ниже, статус «stirring… pulling parts apart»); в конце ядро
  возвращается к номиналу и с этой формы стартуют попытки. Если после финала
  сдвинуты ползунки Thick./Repel. и ядро стало шире зазоров (E=∞), Stir не
  отказывает (v2.08), а сдувает ядро под текущие зазоры и раскрывает заново в
  стадии A. Приём шага стадии A: E не выросла ИЛИ ближайший зазор до чужих
  прядей не уменьшился (E у ядра гиперчувствительна к поправке длин; E0 берётся
  после проекции длин). По нажатию в 3D-окне
  анимируется хаотичное топологически безопасное разрыхление финальной формы
  (`stirStart/stirStep/stirFinish`, `STIR_STEPS=300`, по 6 шагов в кадр):
  случайное гладкое поле — нить режется на 3–6 частей, каждая тянется в свою
  случайную сторону, плюс радиальное расширение 0.6·(r−c)/R, поле сглажено
  вдоль нити (I+200Δ)⁻¹ и спроецировано на касательное пространство жёстких
  длин; каждый шаг под теми же сертификатами непересечения, что и шаг физики
  (кап 0.35·gmin, полное смещение после проекции длин < 0.5·min(gmin, 5s+0.5·L0),
  зазор > 0.55·min(gmin, s), E конечна) — пряди не проходят друг сквозь друга,
  тип узла сохраняется (проверяется точным определителем в конце, при
  расхождении — «⚠ knot type changed by stir»); упор — новое случайное поле,
  смена поля каждые 40 шагов. Затем разрыхлённая форма становится лифтом новой
  серии из «Starts» попыток (окна результатов и список энергий заново), в
  статус добавляется « · stirred ×R» (рост радиуса). Во время разрыхления
  «▶ Physics» и ползунки не действуют; Clear и новая диаграмма прерывают
  разрыхление. Поле шага (v2.05) = случайная часть + сила отталкивания
  (только она, `energyGrad(true,true)`, вес 2) — контакты раскрываются первыми;
  шаг принимается только если минимальный зазор не опускается ниже
  max(1.02·s, 0.55·gmin) (стягивающие направления отбрасываются и поле
  перевыбирается). Если радиус вырос < 2 % или отвергнуто > 90 % шагов, серия не
  запускается, статус: «stir: knot too tight to loosen (…) — reduce
  Thick./Repel. or draw larger» (v2.07: критерий — ядро не раскрылось (<1.05) и
  нить сдвинулась < 2·L0). Радиальная составляющая поля — 1.0·(r−c)/R; порог
  зазора в стадии B — только конечность энергии (чужие пряди дальше текущего
  ядра) и 0.55·gmin. Пометка серии: « · stirred (core ×k)». Проверено при
  Thick 10 / Repel 10 на случайном узле с 12 пересечениями: зазоры 1.26·s →
  4.4·s, форма заметно раскрыта, det сохранён, серия вернулась к минимуму;
  плотные узлы: ядро ×3…×12, пятилистник/семилистник/случайный 25 — det
  сохранён. Пометка « · stirred ×R» и предупреждение о типе узла держатся
  всю серию (`_stirNote/_stirWarn`); растяжение стартов после Stir мягче
  (×0.7…×1.4 — рыхлый лифт при сжатии ×0.4 заклинивал); Clear и новая диаграмма
  отменяют разрыхление и возвращают «▶ Physics». Стенд: `test/stir.js`,
  `test/stir2.js` (плотные: пятилистник ×1.08, семилистник ×1.07),
  `test/jamcheck2.js`, люк `window.__knotStir(k)` (без k — полный бюджет),
  `__knotTrace().stir` = {steps, rejected, rejInf, rejLoc, fields, moved_L0,
  disp_R, disp_L0, grow, gminMin_s, det0, det1, tooTight} (v2.21).
  Проверено: трилистник/тривиальный/случайный узлы — det до/после равны,
  минимальный зазор ≥1.0·s, радиус ×1.29–1.41, энергия ×1.6–3.3, серия после
  разрыхления приходит к прежним минимумам.
- Смена ползунков ВО ВРЕМЯ СЧЁТА (v2.30, по заданию «если во время работы
  ползунки изменены, надо продолжить работу гладко, с новыми константами и
  закончить вычисления»): серия НЕ перезапускается. Новые константы
  применяются к текущей форме на ходу (`applySettingsLive`: ядро сдувается под
  нынешние зазоры и заново раскрывается ратчетом, память спуска, окна покоя,
  чекпойнт фазы A и счётчик выходов из седла сбрасываются); в параллельной
  серии работающие воркеры получают сообщение `{type:'set', thick, rep, bend}`
  (цикл воркера уступает очередь сообщений после каждого снимка — `_yield`
  через MessageChannel), ещё не запущенные попытки стартуют сразу с новыми
  константами. Попытки, закончившиеся ДО смены, досчитываются от своей формы
  без фазы A и без нового лифта (параллельно — задания `resume` в очереди
  воркеров `parRedoAll`, `_par.jobs/queue/total`; последовательно —
  дополнительные раунды после основных `seqMarkStale/msNextRedo/msRecordRound`,
  `_msStale/_msRedo`), в том числе перенесённая после Stir попытка №1; до
  досчёта их окна подписаны «updating»/«recomputing», а энергии не участвуют
  в «Min». Итог серии — равновесия при новых ползунках, пометка
  « · settings changed mid-run: tries finished with the new settings».
  Проверено: стенд `test/slideredo.js` (смена после двух законченных попыток,
  4 попытки, 2 узла) и браузер (пул из двух воркеров: 2 готовы, 2 считаются,
  1 в очереди) — ни одного «does not fit», энергия каждого окна совпадает с
  энергией его формы при новых константах до 10⁻⁹; `test/slidercont.js`
  (смена в первом раунде) — без перезапуска и заклиниваний.
  Ключ настроек (v2.32, по второму ревью — 15 подтверждённых): счётчик
  поколений 2.31 заменён КЛЮЧОМ значений ползунков «w|r|b» (`constsKey`).
  Причина: за 350 мс задержки ползунок уже поменял значения, а номер ещё
  старый — отсюда целый класс дыр (запуск попытки с новыми константами под
  старым номером, окружность в этом окне, перенесённая попытка при старте
  серии). Теперь результат устарел ⇔ его ключ не совпадает с нынешними
  значениями ползунков: окна рассогласования нет по построению, а если
  ползунок вернули к прежнему значению, прежние результаты снова годны.
  Воркер присылает ключ СВОИХ констант в `done`; главный поток в
  последовательном режиме применяет ползунок сразу, поэтому раунд помечается
  нынешним ключом. Прочее по ревью: при окружности устаревшие результаты —
  «skipped», лучший — среди нынешних; запасной путь берёт перенесённую
  попытку из её задания и убирает осиротевшее окно; его пометка живёт до
  конца серии (`_fbNote`); упавший досчёт повторяется один раз и не считается
  «did not fit»; прогресс делится на число заданий; пауза с перенесённой
  попыткой и одной новой — тоже серия. Проверено в браузере: `done` со старым
  ключом во время ещё не истёкшей задержки — досчитан; ползунок увели и
  вернули — ничего не пересчитано, пометки нет; окружность со старым ключом
  на тривиальном узле — досчитана, затем остановка серии с нынешним ключом.
  Доводка по третьему ревью (v2.33, 9 подтверждённых, все средние и мелкие):
  повтор упавшего досчёта сбрасывается при каждом новом досчёте (`retried`);
  если все попытки упали (отказ воркеров), статус говорит «N tries failed
  (worker error) — press ▶ Physics to recompute» и показывает лучшую из
  прежних форм, а не «не помещается» и не сырой лифт; воркер помнит, с какими
  значениями запущен (`ent.key`), и получает `set`, даже если ползунок вернули
  к прежним значениям, пока он стартовал с промежуточными; пометка запасного
  пути сбрасывается в новой параллельной серии; бюджет шагов воркера не
  обрывает раскрытие ядра после смены (до 2× бюджета, заклинивание решает сам
  спуск); концы последовательной серии используют «тихий» сброс задержки
  (`physFlushQuiet`: ползунок уже применён на ходу, счётчики заклинивания не
  сбрасываются — раньше это давало лишнюю фантомную попытку) и общие пометки
  (`msEndNotes`); ещё не досчитанный результат, чьи значения ползунков вернули,
  восстанавливается без пересчёта (последовательно и в очереди воркеров;
  `test/slideback.js`); при паузе и остановке по окружности лучший выбирается
  только среди результатов при нынешних значениях (`parBestAndStale`).
  История 2.31 (для понимания): `_setGen` рос при каждой РЕАЛЬНОЙ смене ползунков
  (`constsCommit` сравнивает с применёнными `_physConsts`; вернули те же
  значения — ничего не происходит); каждый результат попытки помнит поколение,
  при котором посчитан (последовательно `_msGen[место]`, параллельно
  `gen` из сообщения воркера `done` = поколение констант, с которыми он реально
  считал). Серия не заканчивается, пока есть результаты старого поколения: их
  досчитывают от формы. Так закрыты: попытка закончилась за миг до получения
  новых констант или её `done` уже в пути (приходит со старым поколением →
  досчёт); `set` пришёл воркеру после конца попытки (игнорируется — раньше
  давал ложное «does not fit»); смена на паузе, смена в последние 350 мс перед
  концом серии, смена во время Stir (перенесённая попытка №1 досчитывается);
  кнопка Pause применяет отложенную смену, а не выбрасывает её. Главный поток
  при протяжке подстраивает спуск на каждом движении (`applySettingsLive`;
  при разрыхлении `stirDeflateLive`), рассылка воркерам и досчёты — один раз
  после паузы 350 мс. Воркер уступает очередь раз в 0.1 с (в Safari порт
  сообщений идёт через главный поток), после конца попытки — сразу `done`.
  Сторож воркера и его сообщения действуют только для «своего» воркера (досчёт
  идёт под тем же номером задания). Окно устаревшей попытки теряет энергию до
  досчёта (не рамка лучшего, не кандидат для Stir). Проверено: стенд
  `test/slideredge.js` (те же значения; пауза → смена → продолжение; смена за
  миг до конца серии), браузер — искусственная гонка: `done` со старым
  поколением сразу после смены → попытка досчитана; во всех случаях энергия
  каждого окна = энергия его формы при текущих константах (≤2·10⁻⁹).
  Прежнее поведение
  (v2.26–2.29): серия перезапускалась, и свежие лифты при увеличенном ядре
  иногда не раскрывались — отсюда жалобы на окна «jammed»/«does not fit».
- В покое смена ползунков только меняет параметры; физика запускается кнопкой.
  Во время счёта (v2.26)
  применение новых констант откладывается на `PHYS_CHG_MS`=350 мс после ПОСЛЕДНЕГО движения
  ползунка (`physSettingsChangedSoon`; значение и подпись применяются сразу,
  нажатие кнопки и Clear отменяют отложенный перезапуск, `window.__physFlush()`
  применяет его немедленно — для стенда). Причина: `oninput` приходит на каждый
  пиксель протяжки, а каждый перезапуск серии убивает и создаёт заново K
  воркеров, каждый из которых разбирает весь текст страницы; десятки событий
  подряд подвешивали вкладку, сторож объявлял воркеры мёртвыми, серия срывалась
  в последовательный режим и показывала одно окно «jammed» (жалоба со
  скриншотом). Там же: сторож воркера 4 с → 8 с, и таймаут больше не выставляет
  `window.__noWorkers` навсегда — последовательный путь только для этой серии
  (пометка « · worker timed out, sequential»); насовсем воркеры отключаются лишь
  при настоящем отказе (file://, отказ конструктора, ошибка загрузки).
  Стенд: `test/slider.js` (20 событий протяжки → 0 перезапусков, после flush —
  ровно 1, серия доходит до конца; в покое перезапуск немедленный); в браузере:
  40 событий за время серии → 0 перезапусков, после задержки ровно 1, воркеры
  живы, серия продолжается.
  Утоньшение до посадки (v2.34, по заданию «если узел не помещается, надо в
  процессе непрерывно уменьшать его толщину, пока не уместится»): попытка больше
  никогда не заканчивается «does not fit». Если ратчет надувания ядра застрял
  (`_inflStall≥1000`, в т.ч. ползучесть <3 % за 500 шагов) или спуск затих при
  недонадутом ядре, `thinToFit()` фиксирует наибольшую долю толщины, при которой
  узел поместился (`_thickFit *= _inflate`; ядро (w+r)·u и трубка w·u
  масштабируются вместе), и попытка досчитывается обычной физикой. Каждая
  попытка и каждая смена ползунков снова начинают с полной толщины. Результат
  помнит долю толщины (`_msThick`, `thick` в сообщениях воркера); окно
  подписано «E 0.123 · 62% thick», в итоге «@62%»; лучшая попытка выбирается
  сначала по толщине (толще = ближе к заданной), при равной — по энергии
  (`betterRes`); главный вид после серии показывает лучшую попытку с её
  толщиной. Пометки: «knot thinned to NN% to fit (reduce Thick./Repel. for full
  thickness)» или «N of K tries thinned to fit (not compared)»; во время счёта —
  «thinned to NN% to fit». Обратный рост (v2.36): толщина первого застоя — не
  предел (лифт сжат, зазоры малы), поэтому после релаксации при уменьшенной
  толщине узел снова пробует толще (`_thickGrow`, шаг `_thickStep` 1.15,
  при удаче ×1.3 до 2, при упоре снова 1.15; ратчет надувания от текущей
  толщины; до 40 попыток; `_thickDone`, когда упёрлись на минимальном шаге в
  ту же толщину) — итог: наибольшая толщина, при которой узел помещается.
  Пример: 100 пересечений при 10/10 (L/s=150) — первый застой при 3.6 %,
  рост до ≥19 %; 60 пересечений при тех же ползунках помещаются целиком.
  Доводка по ревью (v2.37): возобновление той же попытки после паузы и
  движение ползунка Stiff. не сбрасывают утоньшение (иначе ядро сдувалось и
  усиленный хвост расталкивал уже севший узел); сброс — только при смене
  Thick./Repel. (`_coreKey`). Живой «Min» и выбор прежней формы при паузе
  тоже сравнивают по толщине (`betterRes`). Трубка в окне попытки рисуется
  толщиной этой попытки (третий аргумент `tubeGeometryFor`), а не общей;
  воркер сообщает фазу роста (`grow`), и статус пишет «trying NN% thick» /
  «thinned to NN% to fit». Совет «draw larger» убран из сообщений: посадка
  не зависит от масштаба рисунка (единица — доля длины нити).
  Шаг роста (v2.38): 1.15 после упора, ×1.3 (до 2) после каждой удачи;
  множитель ставится в ратчете при удавшемся росте, а не в начале попытки —
  иначе после упора следующая попытка сразу шла шагом 1.5 и узел зацикливался
  «60 % ↔ проба 90 %» до лимита в 40 попыток (наблюдалось в 2.37 на
  100 пересечениях при 10/10).
  Сторож воркера (v2.38): воркер шлёт `alive` сразу при получении `run`
  (до лифта/выворачивания/первых шагов), а сторож 8 с ждёт именно первого
  сообщения; при его срабатывании свежая попытка, как и досчёт, запускается
  повторно один раз (`job.retried`), а не помечается «failed» — на занятой
  машине (100 пересечений, 5 воркеров, стенды Node) первые 12 шагов до
  первого `progress` занимали дольше 8 с.
  Прежняя подпись «does not fit» (v2.29) осталась только для результатов
  старых сборок. Если не поместилась ЧАСТЬ попыток, в итоговом статусе теперь есть
  подсказка «N of K tries did not fit (reduce Thick./Repel.)» — раньше она
  появлялась только когда не помещались все. Проверено: ползунки 2/8/7 (ядро
  (2+35)=37 единиц) и 25 движений Stiff. во время параллельной серии — ни одного
  заклинивания, серия осталась параллельной, все пять попыток дошли до конца;
  заведомо тесная посадка (10/9/10, ядро 60 единиц, 30 пересечений) даёт
  «does not fit» во всех окнах и подсказку в статусе.
  Отложенное выворачивание Stir (v2.23) ждало кадра анимации — в СКРЫТОЙ вкладке
  кадров нет, и Stir висел в «Stirring…», пока вкладку не откроют; с v2.28 рядом
  с кадром стоит запасной таймер (250 мс), он срабатывает и в фоне.
- Любая необработанная JS-ошибка показывается на странице красным баннером;
  если CDN three.js недоступен — отдельное понятное сообщение.

## Лаборатория Stir — `stir.html` (2026-09-13, экспериментальный файл)

> Файл `stir.html` удалён при чистке в 3.3; его последнее состояние (режимы «петли»,
> «инверсия», раздутие, `<select id="stirMode">`) — в истории git, коммит d31965b.
> В `index.html` перенесён только режим раздутия (3.1). Раздел оставлен как описание опытов.

Задача пользователя: «максимально упростить узел» — найти способ Stir, который
выводит релаксацию в другой бассейн (в идеале к абсолютному минимуму). Идея
пользователя: центр O (центр масс), точки Xn с касательной ⊥ лучу O→Xn, пустой
конус вокруг луча за Xn → из точек слева и справа от Xn вытягивается петля внутрь
конуса без пересечений; несколько таких петель растягивают узел в разные стороны,
не меняя топологии; затем — релаксация. `stir.html` — копия `index.html` (v2.38)
с этим Stir вместо выворачивания (выворачивание оставлено для сравнения:
`<select id="stirMode">` loops | invert, `window.__stirMode(m)`); `index.html` не
меняется. Стенд: `KNOT_PAGE=stir.html node harness.js <тест>`; тесты
`test/stirloops.js` (пресеты; перебор изотопии по λ за `window.__stirDebug`),
`test/stirlab.js` (env MODES=loops,loops:nopin,invert,stretch; W R B TRIES IDX NCS
BUDGET PHYS_SEED — все арм-ы с одного S0, контроль — свежие попытки, d_ICP /
sameBasin, выигрыш по новым попыткам). Спецификации — scratchpad сессии
(`stir_loops_spec_v2.md`, `stir_fanpins_spec.md`).

Метод (по спецификации v3 с поправками пользователя):
- Кандидаты — все вершины; 4 направления ⊥ касательной; дуга ±m вершин
  (m≈0.125·Rk/L0); монотонность проекции дуги вдоль хорды; ранжирование по
  нагрузке контактов Σθ²·(1+n_c).
- Петля — «веер» в конусе 25°: q → q + h_q·(u + tanα/w·P_q), борта расходятся на
  ширину конуса; высота H = U(1.5, 6)·|O·Xn| (случайно), первая петля берёт весь
  остаток бюджета длины, вторая — только если укладывается в диапазон; 1–2 петли.
- Сертификаты (3D, движение каждой точки линейно по λ): пустота развёртки
  (чужие точки ≥ c от каждой образующей), дуга–дуга и дуга–ближняя цепочка —
  выборка по λ с липшицевой оценкой, `stirResampleCert` при ресэмплинге; det —
  канарейка. Проверка перебором в стенде: минимальное расстояние между
  движущимися и неподвижными рёбрами > 0.02·L0 на всех пресетах и rand30 при
  Stiff.=1.
- Переразбиение: узел удлиняется, n′ = round(L′/L0) (тело сохраняет
  разрешение; ядро u = L/3000 растёт, раскрывается в фазе надувания).
- Фаза 1: дальние точки петель (шапка) жёстко закреплены (силы обнулены,
  маска подвижности в `projectLengths`/`dirTangentProject`, `recenter`
  пропущен), обычный отжиг до плато (E падает <0.1 % за 400 шагов после отжига);
  фаза 2: закрепления сняты, новый отжиг (`profilesStart`); фаза 3: масштаб к
  исходной длине, сплайн-ресэмплинг к N₀ с сертификатом, дорелаксация — E
  сравнима с S0 (при отказе сертификата — N′ и E·N′/N₀ с пометкой).
  Чекбокс `#stirPin`, `window.__stirPin(b)`; воркеры получают `s0.mode/pin/N0`.

Замеры (18 случайных диаграмм 20–62 пересечений, ползунки 3/1/9, 5 попыток,
выигрыш = (minS0 − min новых попыток)/minS0, контроль — 4 свежих лифта):
- Фаза-1 версия (борта параллельно, без закреплений): loops 9/18 побед над
  контролем, +0.74 % в среднем, улучшение >0.5 % в 5/18; invert 9/18, +0.75 %;
  stretch (случайное растяжение S0) 9/18, +1.0 %; контроль −2.0 % (медиана
  +0.24 %, >0.5 % в 8/18). Пальцы втягивались за 175–475 шагов — тот же бассейн
  (sameBasin 0.65). Силовая «привязь» вершин петель не помогла (вытягивала
  материал до шпильки) — удалена.
- Веер (H = 1.5–6·ρ, переразбиение, фаза 3), те же 18 диаграмм и тот же S0
  (PHYS_SEED): без закреплений — 13/18 побед над контролем, +2.1 % в среднем,
  медиана +0.74 %, улучшение >0.5 % в 9/18, максимум −18 % (idx 3: 0.268→0.219);
  с закреплениями — 11/18, +1.5 %, медиана +0.75 %, максимум −8 %. Парное
  сравнение (|Δ|>0.2 %): веер лучше выворачивания в 8 диаграммах и хуже в 0,
  лучше растяжения 7:0, лучше параллельных петель 8:0, лучше контроля 11:3.
  Закрепления фазы 1 в среднем не помогают (idx 10: −1.7 % при закреплении).
  det сохранён во всех 180 попытках, заклиниваний нет; фаза 3 сертифицирована
  везде. Свежие лифты (контроль) всё же дают лучший минимум в 5/18 диаграмм —
  разумная стратегия для index.html: серия свежих попыток + веерный Stir от
  лучшей. Время: попытка при N′≈1.6–1.9·N₀ и трёх фазах — 5–8 тыс. шагов
  (в 2–3 раза дольше обычной).
- Петли вдвое уже и больше (2026-09-14, по просьбе: STIR_ARC 0.125, STIR_K 2–4,
  STIR_LEN 2.0 — n′ до 3·N₀; физика уже с Δc=3s, S0 те же в обоих прогонах):
  с закреплениями 14/18 побед над контролем, +3.8 % в среднем (медиана
  +0.33 %, >0.5 % в 8/18, максимум −18.6 %) против 13/18, +2.5 % (медиана
  +0.20 %, 6/18) у прежних широких петель; парно уже/больше лучше в 5
  диаграммах, хуже в 1; без закреплений разница мала (+2.7 % против +2.4 %) —
  с несколькими узкими петлями закрепления фазы 1 начинают помогать (idx 3:
  −18.6 % против −8.7 %, idx 10: −4.3 % против −0.2 %, idx 15: −10.5 % против
  −0.2 %). Серия Stir 290 с против 230 с (N′ до 1800). Принято по умолчанию.
- Stir «раздутие ×k» (идея пользователя: раздуть узел вокруг центра масс в k раз,
  затем обычная физика; в модели толщина ∝ длине, поэтому раздутие = ядро с 1/k
  при той же форме S0). Без расписания ратчет возвращал ядро к номиналу за один
  шаг (эффекта нет); с расписанием (удержание 1/k 600 шагов, геометрический
  подъём к 1 за 1500, затем отжиг заново; клапан ставится после принудительной
  ступени ратчета; `STIR_INFL`, `<input id="stirInfl">`, только stir.html) на тех
  же 18 диаграммах и том же S0: ×10 — 8/18 побед над контролем, +1.0 % в среднем
  (медиана +0.17 %, >0.5 % в 4/18); ×3 — 9/18, +0.9 %, 6/18; парно против узких
  петель с закреплениями — хуже в 9/18 (×10) и 8/18 (×3), лучше в 0–1; в тот же
  бассейн возвращаются 53–64 % попыток. Зато в 5 раз дешевле петель (62 с
  против 290 с на серию). Вывод: раздутие — слабое возмущение (узел с тонким ядром
  сжимается в компактную версию той же формы и раскрывается в тот же бассейн);
  петли остаются лучшим Stir; в index.html не переносится.
- Отжиг с широкими пределами (annlab.js, `window.__ann`, 18 диаграмм):
  ×100/×100 (1500 шагов) медиана −0.01 %, 5 лучше/6 хуже; ×100/×100 (3000)
  −0.16 %, 6/9; ×1000/×1000 (3000) −0.02 %, 7/6, время ×1.5; случайный размах
  на попытку ×32…×1000 (`mixTo`) медиана +0.16 %, 9/8; тот же с 3000 шагами
  +0.05 %, 8/6. Лучший минимум распределился поровну между вариантами.
  Вывод: базовый отжиг (×32/×16, 1500, гладкие профили) остаётся; широкие
  пределы увеличивают разброс, а не снижают минимумы.

### Скорость ядра (2026-09-14; v2.39 — перенесено в `index.html`)
Профиль (rand30, N≈617): `_closestSeg`+`_clamp` ≈35 %, парный цикл ≈30 %, но
главное — `recordTelemetry` каждый шаг вызывал `knotDiagnostics()` с полным
перебором пар O(N²) (94 % вызовов `_closestSeg`). Сделано (стенд
`test/bench.js`: хэш траектории после 1500 шагов — точная эквивалентность;
`FULL=1` — до покоя): (1) безаллокационное расстояние отрезок–отрезок на плоских
массивах (`_segDist`, `_flatCoords`); (2) список соседей Верле (кожа 1·L0,
перестройка при сдвиге любой вершины > кожи/2, проверяется в каждом вызове
`energyGrad` — любые внешние сдвиги покрыты; порядок обхода пар воспроизводит
сеточный, иначе меняется траектория) и точный адаптивный сканер зазоров
`_gapScan` вместо O(N²) в диагностике/`minSegGap(Rep)`; (3) кэш факторизации
циклической трёхдиагональной матрицы Соболева; `jacobiEig` на плоских
массивах (седловая мода и так считается только при покое); (4) минимальный
зазор телеметрии — из последнего вычисления энергии. Итог при тех же
траекториях (хэши совпали): 0,68→0,26 / 3,36→0,97 / 4,04→1,39 мс/шаг
(трилистник / rand30 / rand60), полные попытки 1,3→0,5 с, 9,6→2,8 с, 15→6 с.
Исследование близкодействия (копия `stir_cut.html`): манхэттенский, L∞ и AABB
предфильтры медленнее сферы по серединам (+1…+20 %) — сфера оставлена;
обрезка хвоста Δc 4s→3s меняет энергию равновесий ≤0,006 % (2,5s: ≤0,014 %),
силы — в третьем знаке нет, а пар на 20–30 % меньше: принято `TAIL_CUT=3`
(сертификаты шага — `(1+TAIL_CUT)·s+0.5·L0`); траектории при этом другие
(хэш меняется, выбор бассейна — в пределах шума seed-ов). После обрезки:
0,25 / 0,79 / 1,14 мс/шаг, полные попытки 0,4 / 1,9 / 4,7 с — в 3–5 раз
быстрее исходной сборки. Перенесено в `index.html` как v2.39 (workflow
`wf_8c9d6020-9d0`): хэши стенда совпадают со `stir.html` побитово на каждой
стадии (сначала без обрезки — с прежними хэшами index.html, затем с
`TAIL_CUT=3` — с хэшами stir.html), регрессия (battery, status22, ползунки,
stir8 — выворачивание, corners, jamwhy, thinfit, wsyntax) зелёная; чистые
замеры index.html: 0,68→0,26 / 3,41→0,81 / 4,14→1,17 мс/шаг.

## Версия 3.0 (2026-09-14): петли в основной программе, окна попыток, без автопересчёта
- `index.html` = `stir.html` + косметика: Stir «петли наружу» (веер в конусе 25°,
  H = 1.5–6·|O·Xn|, 2–4 узкие петли, переразбиение n′, фаза 1 с закреплёнными
  шапками, фаза 2 свободная, фаза 3 — возврат к N₀) стал основным режимом Stir;
  выворачивание осталось в `<select id="stirMode">` (loops | invert). Чекбокс
  «pin» в основной программе не показывается (STIR_PIN=true, люк
  `window.__stirPin(b)`); `stir.html` теперь пересобирается из index.html
  (отличия — только title, тег сборки и надпись «Stir lab»).
- Окна попыток: окно, чей узел показан в главном 3D-виде, несёт класс `shown`
  (яркий контур); лучшее (наименьшая E, `msMarkBest`) — зелёная рамка +
  внутренний зелёный контур (`.msWin.best::after`), число энергии жирным
  зелёным и приписка « Min» («E **0.259** Min · 59% thick»); одна подпись через
  `msWinCaption/msWinCapSync`, чтобы класс и текст не расходились. Клик по
  окну (`msSelect`) показывает его узел в главном виде (`_msSel`, `_msShown`,
  `msShowWin`: verts/N/L0 этой попытки через `_setVertsFrom`, трубка и камера);
  для идущей попытки главный вид следует за её воркером (`parPickPreview`
  уважает `_msSel`); выбор снимается новым Physics/Stir, при упавшей/пропущенной
  попытке и не допускается во время счёта главного потока или паузы
  последовательной серии (иначе форма попытки терялась бы). Двойной клик по
  заголовку — развернуть/свернуть (одиночный больше не разворачивает). Stir
  стартует от узла в главном виде (т.е. от выбранной попытки).
- Ползунок Thick. всегда перестраивает трубку главного вида (посчитан узел
  или нет), окна попыток хранят свою толщину. **Автопересчёт отключён**:
  смена ползунков никогда не запускает счёт и не ставит законченные попытки
  на досчёт (`parRedoStale`/`seqMarkStale` — no-op, `_msStale/_msRedo` пусты);
  идущие попытки по-прежнему получают новые константы на ходу ('set'
  воркерам, `applySettingsLive`), пометка серии « · settings changed mid-run
  (finished tries not recomputed)». Пересчёт — только по Physics/Stir.
- Тесты: `test/uiwins.js` (27 проверок: маркеры, подписи, выбор, толщина без
  счёта, пауза, длина нити после Physics из выбранного окна), `test/uisel.js`
  (фальшивые воркеры: упавшая/пропущенная попытка снимает выбор), тесты
  ползунков переписаны под новую семантику; регрессия и хэши стенда без
  изменений. Ревью (workflow `wf_57fd339e-5ac`): 7 подтверждённых замечаний,
  исправлены (выбор во время паузы, L0 выбранной попытки, упавший воркер,
  попытка в очереди).

## Версия 3.1 (2026-09-14): Stir = раздутие, лимит 250, интерфейс
- **Stir в основной программе = «раздутие ×k»** (решение пользователя; лаборатория
  показала, что петли сильнее, но пользователь выбрал раздутие как основной ход):
  чип «Stir ×[k]» (число, по умолчанию 10), выбора режима нет (`STIR_MODE='inflate'`;
  код петель/выворачивания остаётся в файле, `stir.html` показывает `<select>` с
  тремя режимами). Механика: форма S0 без изменений, ядро стартует с 1/k
  (`_inflStart`), удерживается `STIR_INFL_HOLD=600` шагов, поднимается
  геометрически к номиналу за `STIR_INFL_RAMP=1500` (клапан `cap` в ратчете после
  принудительной ступени; застой/ползучесть при упоре в расписание не считаются),
  затем отжиг заново; воркеры получают `s0.mode/infl`.
- **Лимит пересечений 250** (поле Random, `randomKnot`); число вершин лифта: до
  100 пересечений — как прежде (`min(600, 24·C)`), дальше +6 на пересечение
  (250 → 1500). Ограничения: генератор случайных диаграмм упирается в размер
  холста (~165 пересечений при шаге 1.5 радиуса маркера); при ≥150 пересечениях
  3D-лифт реализует другой узел (det не совпадает) — плотные диаграммы требуют
  доработки лифта; физика при N≈1900 — ~20 мс/шаг.
- **Начальная толщина по числу пересечений** (`autoThick`): при создании узла
  (Random, замыкание кривой, пресет) Thick. = 5 → линейно к 1 при 100; далее
  ползунок ручной. Стенд ставит `window.__noAutoThick` (тесты задают толщину
  явно; хэши bench.js не меняются). `autoThick` не перестраивает трубку сам
  (rebuildTube из него менял траекторию стенда) — трубку строит вызывающий.
- **Интерфейс**: окна попыток — выбранное (показанное в главном виде) с жирным
  светлым контуром по краю (`outline`), лучшее — с жирным зелёным контуром чуть
  внутри (`::after`, inset 5 px), подписи отодвинуты (заголовок 28 px, отступ 13 px);
  скругления кнопок, полей ввода и чипов 8/4 px (окна узлов — 10 px как были);
  `<select>` в стиле полей; панели инструментов переносят элементы при нехватке
  места; органы управления 3D-панели компактнее; **разделитель панелей**
  (`#splitter`: тянуть мышью, `--leftW` на `#app`, localStorage `knot.leftW`,
  двойной клик — 46 %; без preventDefault на pointerdown, иначе dblclick
  глушится); параллельных воркеров до 16 (`min(K, cores−1, 16)`).
- Патчи (воспроизводимые, scratchpad сессии): patch_inflate.py, patch_inflate2.py,
  patch_ui31.py, patch_split.py, patch_31b.py (--index), regen_stir.py.

## Версия 3.2 (2026-09-14): WebAssembly-ядро пар
- Парный цикл `energyGrad` (расстояния отрезок–отрезок по списку Верле в
  сеточном порядке, арк-фильтр, хвост отталкивания, ядро, накопление сил,
  gmin/gminRep/gmin3) вынесен в модуль WebAssembly, собранный из AssemblyScript
  (`test/wasm/pairs.ts`, `test/wasm/build.sh` → `pairs.wasm` 2128 байт →
  base64 2840 символов в константе `WASM_B64` внутри области, из которой
  собирается исходник воркера). Без SIMD — совместим со всеми браузерами с 2017.
- Память: одна `WebAssembly.Memory` на поток, арена `_wa` (координаты,
  середины, силы, ячейки, список Верле, `_kRmul`, результаты); JS-массивы
  `_fx/_fy/_fz/_vx…/_vlStart/_vlJ/_gcX…` становятся представлениями арены
  (`_waViews` пересоздаёт их после `grow`; `trueEnergy` восстанавливает
  отсоединённое представление `_kRmul`). `pairPass(...E0)` продолжает сумму E
  от изгибной части — тот же порядок операций ⇒ результаты побитово те же:
  хэши стенда −1541363460 / −286394620 / 1614448689 совпадают с Wasm, с JS
  (`NOWASM=1`) и без WebAssembly вообще (`NOWASM=2`, обычные массивы).
- Запасной путь: JS-цикл остаётся; используется при отсутствии/ошибке
  WebAssembly, `window.__noWasm`, режимах сбора зазоров/контактов Stir,
  `_pairBrute`, n<64. Воркеры ждут `_waInit()` перед стартом; флаг `noWasm`
  идёт в задании. Люки `window.__wasm()` ({ok, used, js, grows, bytes}),
  `__parWasm()` (по воркерам), `__waReady`.
- Скорость (Node, машина без нагрузки): трилистник 0.27→0.24 мс/шаг, rand30
  0.84→0.62, rand60 1.25→0.78 (×1.1/×1.35/×1.6); полные попытки rand30 1.5 с,
  rand60 2.8 с. В браузере все 5 воркеров считают в Wasm (`js:0`).
- Не сделано (по замеру нецелесообразно): перенос изгибного цикла (8–25 %
  шага; `atan2` в AssemblyScript не гарантирует побитового совпадения с V8).
- Стенд: `wasmpairs.js` (модуль против JS pairEval на реальном порядке пар —
  E, gmin, силы бит-в-бит), `wasmgrow.js` (рост арены, 200 пересечений),
  `bench.js NOWASM=…`, `wsyntax.js` проверяет `WASM_B64`/`pairPass`/`await`.

## Версия 3.3 (2026-09-14): рамки окон попыток, чистка папки
- Рамки окон попыток: один жирный контур 3px по самому краю окна — рамка 1px
  в цвете признака + кольцо 2px (`::before`, радиус 9px) сразу внутри неё:
  светлый — окно, показанное в главном виде (`.shown`), зелёный — лучшее
  (`.best`, подпись «Min»). При обоих признаках светлый контур снаружи и такой
  же зелёный (3px, `::after`, отступ 2px, радиус 7px) сразу внутри него.
  Кольца — наложения (раскладка не меняется), заголовок окна 32px (вид 138px,
  окно по-прежнему 172px), чтобы подпись не касалась колец. Прежний вариант
  (зелёное кольцо с отступом 5px внутри тонкой рамки) смотрелся «двойной
  рамкой» с зазором, а цифры упирались в кольцо.
- Чистка папки проекта: удалены `stir.html` (лаборатория Stir — только в
  истории git, коммит d31965b; поддержка `KNOT_PAGE` в стенде сохранена),
  `publish/`, `publish.zip` (создаются заново при публикации на Netlify),
  `serve.log`, `knot-telemetry.json`, папка `telemetry/` (211 дампов; 9 файлов,
  на которые ссылаются скрипты стенда, перенесены в `test/fixtures/telemetry/`,
  пути в 12 скриптах исправлены), в `test/`: `out/`, `*.out`, `err*.txt`,
  `wf_*.js`, `wasm/knots.json`, отчёты `report-*.html` и `noncanon-3d-*.html`,
  варианты стенда `harness_nr.js`/`harness_pg.js`, `ui/mkvariant.py`,
  `pc_helpers.txt`, `sweep2.done`. Посторонняя папка `market-shapes/`
  перенесена из проекта в `Programming/Claude/market-shapes/` (не удалена).
  `test/node_modules` оставлен: стенд требует пакет `three` (восстанавливается
  `npm install` в `test/`). `test/ui/shot.html` оставлен — быстрый снимок окон
  попыток в браузере (`/test/ui/shot.html?state=run|done`).

## Проект и публикация

- Папка: `/Users/balik/Library/CloudStorage/GoogleDrive-gbalik@gmail.com/My Drive/Programming/Claude/Knots/` (git, ветка main).
- Репозиторий: https://github.com/VBalik/knot-editor (публичный).
  Живой сайт: https://vbalik.github.io/knot-editor/
- Публикация: `cp index.html index.html` → commit → push → принудительная
  пересборка Pages (`POST /repos/VBalik/knot-editor/pages/builds`, токен — в
  macOS keychain через `git credential fill`), дождаться `builds/latest` →
  `status=built <sha>`, проверить live с `?cb=<случайное>`.
